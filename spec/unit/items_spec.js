var Item = require('../backend/db/models').Item;
var server = require('../backend/db/server.server.js');
var mongoose = require('mongoose');

describe("Item", function() {
    describe("should create a new item document with a name, max budget and owner.")
  });
  